// Package engine implements the deterministic game rules machine.
// It currently handles state transitions for player movement and look
// (navigation and visibility). The engine never calls LLM or SLM services.
package engine
